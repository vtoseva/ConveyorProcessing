#ifndef VALIDATION_
#define VALIDATION_

#include <iostream>
#include <string>

bool isInteger(double number);

bool isNumber(const char c);

bool isFunction(const std::string str);

#endif // VALIDATION_